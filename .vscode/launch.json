{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch edge",
            "file": "${workspaceFolder}/lesson.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
